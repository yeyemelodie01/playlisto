{% macro activeMenu() %}
    {%- set routesList = [
        'back_recommendation_index',
        'back_recommendation_edit',
        'back_recommendation_delete',
    ] -%}
    {%- if app.request.get('_route') in routesList -%}
        {{- true -}}
    {%- else -%}
        {{- null -}}
    {%- endif -%}
{% endmacro %}
